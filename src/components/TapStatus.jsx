import Tap from "./Tap";

export default function TapStatus() {
  return (
    <section>
      <h2>Tap Status</h2>
      <section>
        <Tap></Tap>
      </section>
      <section>
        <Tap></Tap>
      </section>
    </section>
  );
}
